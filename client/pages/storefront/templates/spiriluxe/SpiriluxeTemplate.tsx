import React from 'react';
import { TemplateProps } from '../types';
import MinimalistTemplate from '../minimalist/MinimalistTemplate';

// Spiriluxe Template - Work in Progress
// Currently using Minimalist as placeholder while full template is developed

export default function SpiriluxeTemplate(props: TemplateProps) {
  return <MinimalistTemplate {...props} />;
}
